<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Vues de reclamations -->
    <record model="ir.ui.view" id="reclamation_form_view">
        <field name="name">view.reclamation</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <form string="Reclamation">
                    <group>
                        <field name="type_reclamation"/>
                    </group>
                <!-- Reste de la vue -->
            </form>
        </field>
    </record>

    <record model="ir.ui.view" id="reclamation_tree_view">
        <field name="name">view.reclamation</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <form string="Reclamation">
                    <group>
                        <field name="type_reclamation"/>
                    </group>
                <!-- Reste de la vue -->
            </form>
        </field>
    </record>


    <!--  le menu principal  -->
    <menuitem id="main_reclamation_plateforme_menu" name="Plateforme"/> 
        
        <menuitem id="gestion_reclamation_menu" name="Gestion des Reclamation"
                  parent="main_reclamation_plateforme_menu"/>
        <!-- the following menuitem should appear *after*
             its parent clientspec_menu and *after* its
             action client_list_action -->
        <menuitem id="reclamation_menu" name="Réclamation" parent="gestion_reclamation_menu"
                  action=""/>
</odoo>